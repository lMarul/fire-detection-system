{
  "name": "urban-ignite-map",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install": "cd Frontend && npm install --legacy-peer-deps",
    "dev": "cd Frontend && npm run dev",
    "build": "cd Frontend && npm run build",
    "preview": "cd Frontend && npm run preview",
    "lint": "cd Frontend && npm run lint"
  }
}
